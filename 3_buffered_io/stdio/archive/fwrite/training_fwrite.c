#include <stdio.h>

/* size_t fwrite (void *buf, size_t size, size_t nr, FILE *stream)
 *
 * DESCRIPTION:
 * 	size == sizeof( *buf )
 * 	nr == number of size
 * 	write size*nr bytes to stream
 *
 * RETURN:
 * 	nr if success
 * 	<nr if failed
 */
 
